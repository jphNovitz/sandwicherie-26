<header class="sticky top-0 z-40 border-b border-white/0 bg-base-100/80 backdrop-blur-md">
    <div class="navbar mx-auto w-full max-w-screen-xl px-4 py-2 sm:px-6 lg:px-8">
        <div class="navbar-start">
            <a href="{{ path('app_home') }}" class="font-headline text-lg font-black uppercase tracking-tight text-primary no-underline sm:text-xl">
                Sandwicherie 26
            </a>
        </div>

        <div class="navbar-end gap-2 sm:gap-4">
            <a href="{{ path('app_home') }}" class="hidden text-sm font-semibold text-secondary no-underline transition-colors duration-300 hover:text-primary sm:inline-flex">
                Accueil
            </a>
            <a href="{{ path('app_contact') }}" class="text-sm font-semibold text-secondary no-underline transition-colors duration-300 hover:text-primary">
                Contact
            </a>
            <a href="{{ path('app_contact') }}" class="btn btn-primary btn-sm rounded-xl px-4 shadow-[0_18px_40px_-26px_rgba(30,27,25,0.38)]">
                Commander
            </a>
        </div>
    </div>
</header>
